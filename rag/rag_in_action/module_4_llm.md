# Module: 4

## LLMs & Text Generation

### Introduction

- LLM foundation
    - How transformers work and how to structure effective LLM calls
- Transformer workflow & grounding
    - Transformer architecture & iterated LLM workflows grounded in retrieved information

### Transformer Architecture

- Origins of Transformer
    - 2015 - Attention mechanism
    - 2017 - Transformer architecture
        - Attention is all you need
    - 2018 - Pre-trained language models
    - 2022 - LLMs become accessible to general public
- Encoder & Decoder
    - Encoder
        - Processes the original text
        - Develops deep contextual understanding of the text’s meaning
        - Used in embedding models for rich semantic representations
    - Decoder
        - Uses the deep understanding from the encoder
        - Generates new text in target language
        - Most LLMs include only the decoder component, as they just care about text generation
- Vectors
    - Dense semantic vectors
        - First guess of meaning
    - Position vectors
        - Position in prompt
    - Semantic & position vectors sent along together processing
- Attention
    - Attention is a fancy way of saying: which other tokens should have the biggest impact on my meaning
    - Each tokens sees the meaning & position of every other token
    - Input embeddings ⇒ Transformer layers [Attention head] ⇒
        - Head 1: Object relations
        - Head 2: Spatial relations
        - Each head learn abstract patterns, not human defined rules
    - Smaller models use 8 or 16 attention heads, but larger ones might use over 100
- Feed forward phase
    - Input embeddings ⇒ Attention ⇒ Feed forward
        - First guess
        - Second guess
- Iterative refinement
    - Input embeddings ⇒ Attention ⇒ Feed forward ⇒ Updated token embeddings ⇒ Input embeddings
        - First guess: first attention + feed forward
        - Second guess: understanding “dog” as subject of sentence
        - Third guess: connecting to brown & sat
        - Nth guess: final refined representation
            - the brown dog sat next to the red fox
    - Next token
        - Using the refined embeddings, predict the next token using model’s vocabulary
            - the brown dog sat next to the red fox
    - Iterative
        - To generate the next token, the model repeats the entire process from scratch
        - This loop continues until it hits either token limit or end of completion token [EOS]
        - The LLM’s output tokens are de-tokenized and returned to the user as the final response
- Conclusion
    - LLMs can deeply understand information added to prompts through
        - Attention mechanism processing
        - World knowledge in feed forward layers
    - Inherent randomness remains
        - LLMs may randomly ignore injected information
        - Need to control randomness
        - Must confirm LLM ground answers in retrieved information
    - Computational expense
        - Generating single tokens require extensive processing
        - Costs grow with prompt/completion length
        - Each token must examine all others for context
        - Most RAG system costs come from running transformers

### LLM Sampling Strategies

- Peaked distribution model is confident
- Flat distribution model is uncertain
- Greedy decoding
    - Sampling strategy that always selects the token with the highest probability at each step of text generation
    - Deterministic
    - Generic-sounding text
    - Can get stuck in a loop
    - Useful in code completion or debugging
- Temperature
    - Parameter that changes the shape of the distribution generated by LLM
        
        ![image.png](Module%204/image.png)
        
- Advanced token sampling
    - Top-K
        - Pick only from the k most likely tokens, ignoring the rest
        - Top-K of 5 means only the 5 most likely tokens can be chosen
    - Top-P
        - Pick from tokens whose cumulative probability is below some threshold
        - Top-P of 85% includes tokens until their probability is above 85%
    - Top-K vs. Top-P
        - Top-P is more dynamic than Top-K
        - Top-K includes the same number of tokens regardless of the shape of the distribution
        - Top-P includes more or fewer tokens depending on how “certain” the model is
- Token specific strategies
    - Repetitive penalties
        - Reduce the probability of already used tokens, discouraging repetition
        - Helps prevent:
            - Loops (”I think, I think…”)
            - Redundant sentence patterns
            - Overuse of specific words
    - Logit Biases
        - Allow direct manipulation of token probabilities by adding or subtracting values from the model’s raw calculated probabilities
            - Biases can:
                - Filter profanity
                - Boost categories in a classifier LLM
- Using sampling strategies
    - Set temperature at top-p that fits your application’s needs
    - Code or factual domain ⇒ low temperature & top-p
    - Creative domain ⇒ explore higher temperature & top-p
    - Then add repetitive penalties, logit biases, etc… as needs arise

### Choosing Your LLM

- Important LLM characteristics
    - Model size
        - Small model: 1 - 10 billion parameters
        - Large model: 100 - 500 billion parameters
        - Large model can be more capable, more expensive
    - Cost
        - Fixed cost per million tokens, different for input vs. output
        - New & larger models cost more
    - Context window
        - Maximum number of tokens an LLM can process, both prompt & completion
        - You still pay per token
    - Latency & speed
        - Time to first token, tokens per second
    - Training & cutoff date
        - Last point in time in the model training data
        - Later is usually preferable
- LLM quality metrics
    - There are many of quality
    - There are many benchmarks to measure quality
    - There is no single authoritative list
    - Three categories:
        - Automated
        - Human-scoring
        - LLM-as-a-Judge
- Automated benchmarks
    - Evaluate with code
    - Common format is multiple choice tests on various subject
    - MMLU scores 57 subjects from STEM  to humanities
    - Many automated benchmarks exist for wide variety of domains
- Human evaluated benchmarks
    - Anonymous LLMs respond to a prompt, human choose preferred response
    - Uses ELO algorithm to create comparative leaderboards
    - LLM Arena is a popular host of human-graded rankings
    - Captures nuanced quality factors automated benchmarks miss
- LLM-as-a-Judge benchmarks
    - One LLM rates another’s response against reference answers
    - Produces win rate for comparing LLM performance
    - Upside: cheap & flexible evaluation method
    - Downside: judges prefer their own model family (bias)
- Good benchmark qualities
    - Relevant to your projects
    - Difficult to help distinguish between high and low performing models
    - Reproducible with stable outcomes between test runs & verifiable results
    - Align with real-world performance
    - Look out for data contamination
- Benchmark over time
    - Benchmark scores start low
    - Scores rapidly rise to on par with human experts
    - Saturated metrics no longer differentiate models
    - New benchmarks need to be repeatedly introduced
    - Main takeaway: Newer models outperform older ones

### Prompt Engineering: building your augmented prompt

- Messages format
    - Content:
        - The text of the message
    - Role:
        - System: provide high-level instructions to influence LLM behavior
        - User: records prompts send by users
        - Assistant: records responses previously generated by LLM
- How chat template structures the information
    
    ![image.png](Module%204/image%201.png)
    
- System prompt
    - System prompt provides high-level instructions on how LLM should behave
    - Include desired tone and procedures LLM should follow
    - Example system prompt approx 2100 words, about 4 - 5 pages
- Claude
    - High-level instructions
        - Fundamental behavior & knowledge cut-off
    - Tone & personality
        - Define how the LLM should communicate
- Your own system prompt
    - Use these principals to construct your own system prompt
        - Instruct LLM to respond in great detail or answer questions succinctly
        - For RAG apps, tell the model to
            - Use only retrieved docs to answer prompts
            - Judge whether a doc is relevant
            - Cite sources in its response
        - System prompts are added to every prompt your LLM processes
- Prompt template
    - To build your augmented prompt
        - Use a well-considered template since prompts include many pieces of information
        - Templates provide structure & decide where contents get injected
    
    ![image.png](Module%204/image%202.png)
    

### Prompt Engineering: advanced techniques

- In context learning
    - Adding extra information to the prompt
        - Many examples → few-shot learning
        - One example → one-shot learning
        - A common way to implement this is by hard-coding examples
- Encouraging reasoning
    - Another powerful technique encourages the LLM to reason through prompts step by step
- Chain of Thought
    - Input → Generate steps → Follow steps → Output
- Reasoning models
    - Reasoning tokens boost accuracy but add cost
    - These models run slower and are more expensive
    - Worth it in RAG for better relevance and integration
- Reasoning models warnings
    - Many prompting techniques don’t work well on reasoning models
        - Struggle with in-context learning and example mixing
        - Perform best with clear goals & strict formats
        - Work well with full context & high-level guidance
        - LLM providers will suggest the best ways to prompt new models
- Context window management
    - Advanced techniques consume more context
- Context pruning
    - With single-turn conversation skip prompt techniques if they add no value
    - Use context pruning to manage long multi-turn prompts
    - With reasoning models, drop reasoning tokens from chat history
- Management strategies
    - Include only chunks relevant to the latest question
        - Use long-context models for deeper, multi-turn conversations
        - Ever with big context, keep prompts efficient

### Handling hallucinations

- Why LLMs hallucinate
    - LLMs produce probable text sequences
    - Probable text is not always accurate, LLMs can’t tell the difference
- Why hallucinations matter
    - Inaccurate information
    - Hard to detect
    - Erode trust
    - RAG can help
- Types of hallucinations
    - Sometimes mess up small details
    - Other times entirely invent facts
    - Need to evaluate LLM output on many levels to ensure accuracy
- Self-consistency methods
    - Repeatedly generate responses to the same prompt and confirm consistency
    - Factual inconsistencies can indicate hallucinations
    - In practice, costly and unreliable
- Citation generation
    - Instruct LLM to cite sources after each sentence or paragraph
    - Increases likelihood of grounding and makes human verification easier
    - LLMs can just hallucinate citations!
- ContextCite
    - Attributes sentences in response to retrieved docs
    - Tags sentences with supporting doc
    - Can be used to generate citations or as part of evaluation
- Evaluating citation quality in LLMs
    - ALCE benchmark
        - Pre-assembled knowledge bases + sample questions
        - Tests RAG system responses on prepared prompts
        - Evaluates responses across three metrics
    - Key evaluation metrics
        - Fluency
            - How clear and well-written is the generated text?
        - Correctness
            - How factually accurate is the generated content?
        - Citation quality
            - How well do citations align with correct sources?

### Evaluating LLM Performance

- LLM responsibilities
    - Assume retriever finds relevant information, possibly with a few irrelevant docs
    - LLM should respond clearly, incorporate relevant information, cite sources, ignore irrelevant information
    - These responsibilities are subjective
    - LLM evals use other LLMs, e.g. found in RAGAS library
- Response relevancy
    - Measures where response is relevant to user prompt, regardless of accuracy
    - Evaluator LLM generates several new sample prompts that could have lead to the response
    - Embed original and sample prompts to vectors and calculate cosine similarity
    - Average similarity score for final relevancy measure
    - Doesn’t check if information is factual, just if you can work backwards from generated response to original user prompt
- Faithfulness
    - Measures whether response is consistent with retrieved information
    - LLM identifies all factual claims in response
    - More LLM calls to determine if claims are factually supported by retrieved information
    - Percentage of supported claims the the faithfulness
- Other RAGAS metrics
    - Other RAGAS metrics capture noise sensitivity, citation, etc…
    - All metrics rely on LLM-as-a-Judge at some point since LLM role is complex
- Measuring LLM performance indirectly
    - Measure LLM performance by measuring overall system performance
    - Collect system-wide feedback (e.g. thumbs up/down) and A/B test changes to LLM
    - Important to isolate changes to LLM to ensure you’re capturing impact of only those changes

### Agentic RAG

- Agentic systems as flowcharts
    - Agentic systems as flowcharts
        - Each LLM completes one specific task in the prompt journey, taking text input & generating text output at each step
    - Different LLMs for different tasks
        - Use lightweight models for routing & evaluation, larger models for response generation, and specialized models for citations
- Sequential workflow
    
    ![image.png](Module%204/image%203.png)
    
- Conditional workflow
    
    ![image.png](Module%204/image%204.png)
    
- Iterative workflow
    
    ![image.png](Module%204/image%205.png)
    
- Parallel workflow
    
    ![image.png](Module%204/image%206.png)
    
- Implementing agentic systems
    - For simple agentic systems, you can manually implement the workflow logic yourself
    - As complexity increases, consider using tools, libraries, or platforms to build and manage your agentic system

### RAG vs. Finetuning

- Understanding finetuning
    - The core idea of finetuning is to retrain LLM with your own data to update its internal parameters
    - Done through supervised finetuning:
        - SFT retrains the model with labeled examples
        - Instruction tuning teaches task-following behavior
    - Feed instruction ⇒ Compare output to correct answers ⇒ Adjust internal parameters
- When finetuning works well
    - Providing initial medical diagnosis
    - Summarizing legal briefs
    - Small models can be fine-tuned for narrow tasks in agent systems
    - Finetuning only optimizes for the target domain
    - Finetuning does not teach new facts well, it mostly changes how a model talks, not what it knows
- RAG vs. Finetuning
    - RAG
        - Best for knowledge injection
        - RAG is best when the LLM needs new information
        - You can inject context into the prompt and the model will use it
    - Finetuning
        - Best for domain adaptation
        - Finetuning is best for task or domain specialization
        - It’s ideal for one clear, focused task
- Why choose fine-tuning for RAG?
    - Train models specifically to incorporate retrieved information into responses
        - RAG provides current, accurate information
        - Finetuning optimizing how models use that information
        - When deciding whether to use finetuning or RAG, the best choice might be both!

### Conclusion

- Transformer architecture
- Sampling strategies
- Model selection & prompting
- Performance evaluation
- Advanced capabilities